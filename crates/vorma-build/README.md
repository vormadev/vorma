# vorma-build

Build and dev-server support for Vorma applications.

This crate owns the build entry API, dev loop, Vite integration, generated TypeScript
contracts, production manifest generation, and static asset publication used by Vorma
apps.
