# vorma-macros

Procedural macros for Vorma.

This crate is an implementation detail of the `vorma` public API. Applications should use
the macros re-exported by `vorma` rather than depending on this crate directly.
