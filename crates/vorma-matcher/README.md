# vorma-matcher

Standalone path-pattern parsing and matching.

The matcher validates and normalizes pattern text according to caller-supplied syntax
options, detects equivalent or colliding pattern shapes, and resolves single or ordered
nested matches for concrete paths.
