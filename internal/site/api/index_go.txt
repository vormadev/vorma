// For reference:

package api

import (
	"net/http"

	"site/backend"
	"site/backend/src/router"

	"github.com/vormadev/vorma/wave"
)

var siteRouter = router.Core()

func init() {
	backend.Vorma.Init(wave.GetIsDev())
}

func Handler(w http.ResponseWriter, r *http.Request) {
	siteRouter.ServeHTTP(w, r)
}
