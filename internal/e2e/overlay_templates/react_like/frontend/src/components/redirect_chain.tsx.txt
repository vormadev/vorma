import { useLoaderData } from "../vorma.bindings.ts";
import type { RouteProps } from "../vorma.gen/index.ts";

export function RedirectChainStart(
	_props: RouteProps<"/redirect-chain/start">,
) {
	return (
		<section id="e2e-redirect-chain-start-route">
			<p id="e2e-redirect-chain-start-unreachable">
				redirect-chain-start-unreachable
			</p>
		</section>
	);
}

export function RedirectChainMiddle(
	_props: RouteProps<"/redirect-chain/middle">,
) {
	return (
		<section id="e2e-redirect-chain-middle-route">
			<p id="e2e-redirect-chain-middle-unreachable">
				redirect-chain-middle-unreachable
			</p>
		</section>
	);
}

export function RedirectChainEnd(props: RouteProps<"/redirect-chain/end">) {
	const data = useLoaderData(props);

	return (
		<section id="e2e-redirect-chain-end-route">
			<p id="e2e-redirect-chain-hop">{data.hop}</p>
			<p id="e2e-redirect-chain-terminal">{data.terminal}</p>
		</section>
	);
}
