# vormaclient/client Strict Decision Ledger

This file records strict first-principles decisions made during migration. There
is no deferred "open" queue in this process.

## Status Keys

- `accepted`: behavior is explicitly chosen as intended contract.
- `fixed`: implementation and tests were updated to satisfy strict contract.

## Decisions

| ID   | Area                                       | Status   | Strict Decision                                                                                                                                                                                       | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | ------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BC1  | redirect handling                          | accepted | Native browser redirect responses (`response.redirected === true`) for GET must be followed.                                                                                                          | Covered by strict contract in `src/contracts/client.navigation_modes.contract.test.ts` ("follows native fetch redirects for GET").                                                                                                                                                                                                                                                                                                                    |
| BC2  | redirect handling                          | fixed    | Native browser redirect responses for non-GET must also be followed.                                                                                                                                  | Fixed in `src/redirects/redirects.ts` by removing non-GET suppression; covered by `src/contracts/client.submit_and_redirect.contract.test.ts`.                                                                                                                                                                                                                                                                                                        |
| BC3  | redirect loop handling                     | fixed    | Redirect-limit failures must always unwind loading state and clean navigation entries.                                                                                                                | Fixed in `src/client.ts` by deleting aborted navigation entries in `navigate`; covered by strict redirect-cap test.                                                                                                                                                                                                                                                                                                                                   |
| BC4  | mixed loading state policy                 | accepted | `isNavigating` and `isRevalidating` may both be true when operations overlap.                                                                                                                         | Existing strict contract retained in `src/contracts/client.state_and_revalidation.contract.test.ts`.                                                                                                                                                                                                                                                                                                                                                  |
| BC5  | navigation error behavior                  | fixed    | Failed navigation must keep URL/title stable, clear loading state, and keep client operable.                                                                                                          | Enforced by strict assertions in `src/contracts/client.error_and_edge.contract.test.ts`; duplicate top-level log removed in `src/client.ts`.                                                                                                                                                                                                                                                                                                          |
| BC6  | submit error model                         | accepted | Submit failures use deterministic exact error values (`"500"`, `"Network failure"`, `"Aborted"`).                                                                                                     | Enforced by strict assertions in `src/contracts/client.submit_and_redirect.contract.test.ts`.                                                                                                                                                                                                                                                                                                                                                         |
| BC7  | redirect limit semantics                   | accepted | Redirect chains cap at exactly 10 fetches and emit explicit "Too many redirects" logging.                                                                                                             | Enforced by strict assertions in `src/contracts/client.submit_and_redirect.contract.test.ts`.                                                                                                                                                                                                                                                                                                                                                         |
| BC8  | submit dedupe handoff                      | fixed    | Same-key dedupe handoff must not create a false `isSubmitting=false` gap while replacement is active.                                                                                                 | Fixed in `src/client.ts` by guarding submission-key cleanup to owner entry; enforced by submit dedupe-handoff strict contract.                                                                                                                                                                                                                                                                                                                        |
| BC9  | redirect buildID ordering                  | fixed    | Navigation redirects must update/dispatch build ID before following redirect target fetches.                                                                                                          | Fixed in both runtime navigate (`src/client.ts`/runtime outcome handling) and link-click redirect handling (`src/link_navigation_outcome.ts`); enforced by strict contracts in `src/contracts/client.submit_and_redirect.contract.test.ts` and `src/contracts/client.link_click.contract.test.ts`.                                                                                                                                                    |
| BC10 | head section reconciliation                | fixed    | Managed head sections must remove interleaved non-element nodes during reconciliation.                                                                                                                | Fixed in `src/head_elements/head_elements.ts` by removing non-element nodes between markers; enforced by `src/contracts/head_elements.contract.test.ts`.                                                                                                                                                                                                                                                                                              |
| BC11 | client-loader server data                  | fixed    | If manifest marks a route as server-loaded, missing loader payload must hard-stop with `AbortError`.                                                                                                  | Fixed by centralized payload validation in `src/client_loader_server_data.ts` and applied in both `src/navigation_runtime/parallel_client_loaders.ts` and `src/client_loader_work_items.ts`; enforced by strict contract in `src/contracts/client.error_and_edge.contract.test.ts`.                                                                                                                                                                   |
| BC12 | link hash transitions                      | fixed    | Same-document hash removal must be treated as a same-document transition (no route-data refetch).                                                                                                     | Fixed in `src/link_hash_change.ts` by comparing target and current hash for all add/update/remove transitions; enforced by strict contracts in `src/contracts/client.link_click.contract.test.ts` and `src/contracts/client.prefetch.contract.test.ts`.                                                                                                                                                                                               |
| BC13 | navigation slot keying                     | fixed    | Slot lookup/mutation by URL key must remain correct across same-data hash retargeting.                                                                                                                | Fixed by hash-insensitive same-data fallback matching in `src/navigation_runtime/navigation_slots.ts` and `src/navigation_runtime/navigation_slot_mutations.ts`; enforced by `src/navigation_runtime/navigation_slots.test.ts`.                                                                                                                                                                                                                       |
| BC14 | cross-origin hash handling                 | fixed    | Hash-only classification must never treat cross-origin links as same-document transitions.                                                                                                            | Fixed in `src/link_hash_change.ts` by requiring same-origin targets; enforced by strict contract in `src/contracts/client.link_click.contract.test.ts`.                                                                                                                                                                                                                                                                                               |
| BC15 | skip-fetch cache integrity                 | fixed    | Skip-fetch must not run when required cached server-loader data is missing.                                                                                                                           | Fixed in `src/navigation_runtime/skip_server_fetch_result_item.ts` by requiring defined cached loader data for server-loader routes; enforced by strict contract in `src/contracts/client.module_loading_and_fetch.contract.test.ts`.                                                                                                                                                                                                                 |
| BC16 | prefetch timer lifecycle                   | fixed    | Repeated prefetch start calls must not leak orphan timers after stop.                                                                                                                                 | Fixed in `src/link_prefetch_handlers.ts` by suppressing duplicate timer scheduling while one timer is pending; enforced by strict contract in `src/contracts/client.prefetch.contract.test.ts`.                                                                                                                                                                                                                                                       |
| BC17 | loading indicator timers                   | fixed    | Timer-sentinel logic must handle timer id `0` without missing clears or duplicate schedules.                                                                                                          | Fixed in `src/global_loading_indicator/global_loading_indicator.ts` by switching to explicit `null` sentinel checks; enforced by strict contract in `src/contracts/client.loading_and_focus.contract.test.ts`.                                                                                                                                                                                                                                        |
| BC18 | submit request-body policy                 | fixed    | `GET`/`HEAD` and implicit-`GET` (method omitted) submit requests must not carry a request body.                                                                                                       | Fixed in `src/redirects/redirect_request_init.ts` by stripping body when method is `GET`, `HEAD`, or omitted, and rebuilding request init without leaking `requestInit.body` through spread merge order; enforced by strict contract in `src/contracts/client.submit_and_redirect.contract.test.ts`.                                                                                                                                                  |
| BC19 | body-resolution runtime guard              | fixed    | Request-body normalization must not assume `ReadableStream` exists in the global runtime.                                                                                                             | Fixed in `src/vorma_app_helpers/body_resolution.ts` by guarding the `ReadableStream` pass-through check with `typeof ReadableStream !== "undefined"`; enforced by focused tests in `src/vorma_app_helpers/body_resolution.test.ts`.                                                                                                                                                                                                                   |
| BC20 | scroll-state corruption guard              | fixed    | Corrupted persisted scroll-state payloads must be discarded instead of repeatedly re-parsed forever.                                                                                                  | Fixed in `src/scroll_state_storage.ts` and `src/scroll_state_refresh_state.ts` by removing malformed storage entries on parse failure; enforced by focused tests in `src/scroll_state_storage.test.ts` and `src/scroll_state_refresh_state.test.ts`.                                                                                                                                                                                                  |
| BC21 | page-refresh snapshot shape                | fixed    | Parseable but structurally invalid page-refresh snapshots must never be applied as scroll positions.                                                                                                  | Fixed in `src/scroll_state_refresh_state.ts` by validating `x/y/unix/href` snapshot shape before restore and removing invalid payloads; enforced by focused tests in `src/scroll_state_refresh_state.test.ts`.                                                                                                                                                                                                                                        |
| BC22 | scroll-state FIFO eviction                 | fixed    | FIFO eviction must delete the oldest entry even when its key is an empty string.                                                                                                                      | Fixed in `src/scroll_state_storage.ts` by changing oldest-key deletion guard to `firstKey !== undefined`; enforced by focused regression test in `src/scroll_state_storage.test.ts`.                                                                                                                                                                                                                                                                  |
| BC23 | init event registration                    | fixed    | Repeated `initClient()` calls must not register duplicate global beforeunload/touch listeners.                                                                                                        | Fixed in `src/init_client_events.ts` by making beforeunload and touch detection registration idempotent via module guards; enforced by strict contract in `src/contracts/client.history_and_init.contract.test.ts` ("registers beforeunload and touch listeners only once across repeated init calls").                                                                                                                                               |
| BC24 | history init listener policy               | fixed    | Repeated `initClient()` calls must keep exactly one active history listener while still running init side effects.                                                                                    | Fixed in `src/history/history.ts` by cleaning up the previous history listener before re-registering on init; enforced by strict contract in `src/contracts/client.history_and_init.contract.test.ts` (re-init asserts history listener cleanup + re-registration).                                                                                                                                                                                   |
| BC25 | route path encoding                        | fixed    | Dynamic and splat route path values must be URL-encoded before being inserted into resolved paths.                                                                                                    | Fixed in `src/vorma_app_helpers/path_resolution.ts` by URL-encoding dynamic params and splat segments while preserving explicit-index stripping semantics; enforced by focused tests in `src/vorma_app_helpers/path_resolution.test.ts`.                                                                                                                                                                                                              |
| BC26 | hash scroll target decoding                | fixed    | Percent-encoded hash fragments must resolve to the same target element used by browser hash semantics.                                                                                                | Fixed in `src/scroll_state_manager.ts` by decoding hash fragments before `getElementById(...)` lookup and falling back to raw values when decode fails; enforced by strict contract tests in `src/contracts/client.utilities.contract.test.ts`.                                                                                                                                                                                                       |
| BC27 | hash normalization consistency             | fixed    | Hash-only transitions and hash-driven scroll restore must use one shared normalization primitive, not ad hoc parsing.                                                                                 | Fixed by introducing `src/hash_fragment.ts` and replacing ad hoc hash checks/parsing in `src/link_hash_change.ts`, `src/rendering_history_scroll.ts`, and `src/history/history_pop_navigation.ts`; enforced by focused coverage in `src/hash_fragment.test.ts` plus strict contract coverage in `src/contracts/client.history_and_init.contract.test.ts`.                                                                                             |
| BC28 | hash decode idempotence                    | fixed    | Hash fragments used for scroll restoration must be decoded exactly once, never double-decoded.                                                                                                        | Fixed by separating raw fragment extraction from decode normalization in `src/hash_fragment.ts` and using raw fragments in `src/rendering_history_scroll.ts` + `src/history/history_pop_navigation.ts`; enforced by strict contract coverage in `src/contracts/client.history_and_init.contract.test.ts` (literal `%` ID POP case).                                                                                                                   |
| BC29 | delayed prefetch resilience                | fixed    | Delayed prefetch must not become stuck or leak unhandled rejections when `beforeBegin` throws.                                                                                                        | Fixed in `src/link_prefetch_handlers.ts` by clearing timer sentinel on fire, catching delayed prefetch startup errors, resetting `prefetchStarted` on failure, and logging; enforced by strict retry coverage in `src/contracts/client.prefetch.contract.test.ts`.                                                                                                                                                                                    |
| BC30 | same-document link no-op policy            | fixed    | Same-document links targeting the current normalized fragment must be no-op (no client navigation fetch).                                                                                             | Fixed by adding normalized same-document location checks in `src/hash_fragment.ts` and applying no-op guards in `src/link_click_handler.ts` + `src/link_prefetch_click.ts`; enforced by strict contracts in `src/contracts/client.link_click.contract.test.ts` and `src/contracts/client.prefetch.contract.test.ts`.                                                                                                                                  |
| BC31 | prefetch no-op stale-state                 | fixed    | No-op prefetch attempts must not permanently block later valid prefetch attempts for the same handler.                                                                                                | Fixed by deriving `prefetchStarted` validity from actual idle prefetch presence via `hasIdlePrefetchNavigation(...)` in `src/link_prefetch_handlers.ts`; enforced by strict contract coverage in `src/contracts/client.prefetch.contract.test.ts` (current-page no-op then location-change retry case).                                                                                                                                               |
| BC32 | POP hash no-rescroll policy                | fixed    | Same-document POP with unchanged normalized hash target must not force redundant anchor scroll.                                                                                                       | Fixed in `src/history/history_pop_navigation.ts` by gating hash-update scroll on normalized hash-target change; enforced by strict contract in `src/contracts/client.history_and_init.contract.test.ts` (encoding-equivalent POP hash case).                                                                                                                                                                                                          |
| BC33 | POP empty-fragment removal policy          | fixed    | Same-document POP from hash target to `#` must be treated as hash-target removal and restore saved scroll state.                                                                                      | Fixed in `src/history/history_pop_navigation.ts` by deriving removal from normalized hash-target emptiness, not raw hash-string truthiness; enforced by strict contract in `src/contracts/client.history_and_init.contract.test.ts` (`#section` -> `#` POP restoration case).                                                                                                                                                                         |
| BC34 | skip-fetch query order sensitivity         | fixed    | Skip-fetch eligibility must treat query-order changes as changed input, not normalize them away.                                                                                                      | Fixed in `src/navigation_runtime/skip_server_fetch_eligibility.ts` by comparing exact `URL.search` values (instead of sorted search-param entries); enforced by focused tests in `src/navigation_runtime/skip_server_fetch_eligibility.test.ts`.                                                                                                                                                                                                      |
| BC35 | history same-location equivalence          | fixed    | User navigation to an encoding-equivalent same-document hash target must not push a new history entry.                                                                                                | Fixed in `src/rendering_history_scroll.ts` by using normalized same-document location equivalence (via `isSameDocumentLocation(...)`) for push-vs-replace decisions; enforced by strict contracts in `src/contracts/client.navigation_lifecycle.contract.test.ts` (encoding-equivalent hash replace + real hash-change push).                                                                                                                         |
| BC36 | native redirect current-target detection   | fixed    | Native fetch redirects landing on an encoding-equivalent current hash target must be treated as already redirected (`did`) and must not trigger a redundant follow-up fetch.                          | Fixed in `src/redirects/redirect_response_parsing.ts` by using normalized same-document location equivalence for browser-redirect current-target detection; enforced by strict contract in `src/contracts/client.navigation_modes.contract.test.ts` (encoding-equivalent `#~` vs `#%7E` native redirect case).                                                                                                                                        |
| BC37 | page-refresh snapshot applicability        | fixed    | Page-refresh scroll snapshots that are stale or for a non-current URL must be discarded immediately (one-shot semantics) while still treating encoding-equivalent same-document hash URLs as current. | Fixed in `src/scroll_state_refresh_state.ts` by using normalized same-document location equivalence for snapshot URL checks and clearing snapshots when mismatched/stale; enforced by focused tests in `src/scroll_state_refresh_state.test.ts` and strict init contracts in `src/contracts/client.history_and_init.contract.test.ts`.                                                                                                                |
| BC38 | URL identity primitive drift prevention    | fixed    | Hash-insensitive URL identity checks must share one implementation so all navigation paths evaluate data-target equivalence consistently.                                                             | Fixed by promoting `hrefWithoutHash(...)` and `hasSameDataTarget(...)` to shared exports in `src/hash_fragment.ts` and making `src/navigation_runtime/url_identity.ts` re-export those helpers; enforced by focused coverage in `src/hash_fragment.test.ts` (hash-insensitive identity + query-order sensitivity).                                                                                                                                    |
| BC39 | history POP prelude identity consistency   | fixed    | Same-document POP classification in history prelude logic must be derived from the shared URL identity primitive, not ad hoc pathname/search comparisons.                                             | Fixed in `src/history/history_listener_prelude.ts` by classifying POP same-document status via `hasSameDataTarget(...)` over normalized absolute hrefs; enforced by focused tests in `src/history/history_listener_prelude.test.ts` (same-data POP, query-order-different POP, non-POP behavior).                                                                                                                                                     |
| BC40 | global-constructor body guards             | fixed    | Body normalization and redirect request-init logic must never throw when optional web constructors (`Blob`, `FormData`, `URLSearchParams`, `ArrayBuffer`, `ReadableStream`) are unavailable.          | Fixed in `src/vorma_app_helpers/body_resolution.ts` and `src/redirects/redirect_request_init.ts` by guarding `instanceof` and `ArrayBuffer.isView` checks behind global-constructor availability; enforced by focused tests in `src/vorma_app_helpers/body_resolution.test.ts` and `src/redirects/redirect_request_init.test.ts`.                                                                                                                     |
| BC41 | constructor-guard primitive centralization | fixed    | Constructor-availability guard behavior must come from one shared primitive to avoid drift across body-handling code paths.                                                                           | Fixed by introducing shared constructor guard helpers in `src/utils/global_constructors.ts` and reusing them in `src/vorma_app_helpers/body_resolution.ts` and `src/redirects/redirect_request_init.ts`; enforced by direct helper tests in `src/utils/global_constructors.test.ts` and downstream body-handling tests.                                                                                                                               |
| BC42 | detached navigation promise safety         | fixed    | Detached/stale navigation async work must never emit unhandled promise rejections, even when outcomes are dropped before wait-phase consumption.                                                      | Fixed by introducing `observePromiseRejection(...)` in `src/utils/promise_safety.ts` and applying it to detached promise paths in `src/navigation_runtime/parallel_client_loaders.ts`, `src/navigation_runtime/server_success_outcome.ts`, `src/navigation_runtime/client_only_outcome.ts`, and `src/navigation_runtime/navigation_entry_factory.ts`; enforced by strict contract coverage in `src/contracts/client.error_and_edge.contract.test.ts`. |

## Rule

If a new ambiguity appears and first principles cannot resolve it, ask the user
immediately before adding or relaxing assertions.
