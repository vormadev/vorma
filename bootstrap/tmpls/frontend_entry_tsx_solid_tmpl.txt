import { getRootEl, initClient } from "vorma/client";
import { VormaRootOutlet } from "vorma/{{.UIVariant}}";
import { render } from "solid-js/web";
import { vormaAppConfig } from "./vorma.gen/index.ts";
{{.TailwindFileImport}}
await initClient({
	vormaAppConfig,
	renderFn: () => {
		render(() => <VormaRootOutlet />, getRootEl());
	},
});
