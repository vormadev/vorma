Vite is launched without an explicit host, so bind family is left to Vite/runtime defaults (--port only, no --host): cmd.go:107.
Browser dev scripts are hardcoded to http://localhost:<port> for @vite/client, entry module, and React preamble: viteutil.go:135, viteutil.go:152, viteutil.go:160.
SSR route pipeline also hardcodes Vite dev URL to http://localhost:<port>: routepipeline.go:898.
Readiness logic allows either 127.0.0.1 or localhost, so devserver can declare “ready” even when the browser-facing host path fails: reloadwait.go:446.
Tests currently enforce the localhost assumption, so this mismatch is protected instead of caught: viteutil_test.go:114, routepipeline_test.go:1134.

---

Strict comparison complete. Here are the concrete deltas from `main` that can introduce flakiness.

1. Refresh server port is no longer propagated into runtime env for script generation.
- `main` behavior:
  - `main:wave/internal/ki/dev_core.go:35` calls `set_refresh_server_port(refresh_server_port)` before app start.
  - `main:wave/internal/ki/env.go:54` writes `WAVE_REFRESH_SERVER_PORT`.
- Current behavior:
  - [devserver.go](/Users/sjc/__code/river/wave/wavedev/devserver/devserver.go):640-667 starts refresh server and stores `server.RefreshPort`, but never calls `setRefreshServerPort`.
  - [wave.go](/Users/sjc/__code/river/wave/wave.go):1767-1770 reads `__WAVE_REFRESH_SERVER_PORT` and falls back to `10000`.
  - [reloadwait.go](/Users/sjc/__code/river/wave/wavedev/devserver/internal/reloadwait/reloadwait.go):639-651 can fall back to a random port when preferred is unavailable.
- Result:
  - When refresh server falls off `10000`, browser script can still target `10000`.
  - This is a real mismatch path and directly flaky.

2. Refresh server bind host changed in a way that can conflict with browser host resolution.
- `main` behavior:
  - `main:wave/internal/ki/dumb_little_helpers.go:52` listens on `":" + port` (all interfaces).
- Current behavior:
  - [reloadwait.go](/Users/sjc/__code/river/wave/wavedev/devserver/internal/reloadwait/reloadwait.go):636 binds only `127.0.0.1`.
  - [waveruntime.go](/Users/sjc/__code/river/wave/internal/waveruntime/waveruntime.go):212-218 builds websocket URL from `window.location` host and only overrides port.
- Result:
  - If page resolves/uses `::1`, websocket can target `::1:<port>` while sidecar is only on `127.0.0.1:<port>`.

3. Fail-fast startup was replaced with fail-open logging for runtime/Vite startup.
- `main` behavior:
  - `main:wave/internal/ki/dev_core.go:52-54` panics if Vite start fails.
  - `main:wave/internal/ki/dumb_little_helpers.go:185-187` panics if app binary start fails.
- Current behavior:
  - [devserver.go](/Users/sjc/__code/river/wave/wavedev/devserver/devserver.go):973-976 logs Vite start error and continues.
  - [devserver.go](/Users/sjc/__code/river/wave/wavedev/devserver/devserver.go):383-387 logs app start error and continues.
- Result:
  - Dev loop can stay “running” with partially broken runtime, producing intermittent client failures instead of deterministic crash.

4. Readiness failure semantics changed from hard-fail to silent skip.
- `main` behavior:
  - `main:wave/internal/ki/dumb_little_helpers.go:217-225` panics when app/Vite readiness fails before broadcast.
- Current behavior:
  - [reloadwait.go](/Users/sjc/__code/river/wave/wavedev/devserver/internal/reloadwait/reloadwait.go):299-313 logs and skips broadcast.
- Result:
  - Missed reloads can appear intermittently with no hard stop.

5. Readiness timing policy changed significantly.
- `main` behavior:
  - `main:wave/internal/ki/dumb_little_helpers.go:135-156` roughly 3s total wait.
- Current behavior:
  - [appsupervisor.go](/Users/sjc/__code/river/wave/wavedev/devserver/internal/appsupervisor/appsupervisor.go):50-56 uses 25s budget and exponential backoff.
- Result:
  - Much longer hangs before failure signal, which looks like flake from the browser side.

6. Vite port behavior changed (can alter collision profile).
- `main` behavior:
  - `main:kit/viteutil/viteutil.go:163-165` always probes from `5199` (ignores caller default).
- Current behavior:
  - [viteutil.go](/Users/sjc/__code/river/lab/viteutil/viteutil.go):193-195 uses caller default.
  - [viteutil.go](/Users/sjc/__code/river/lab/viteutil/viteutil.go):173-181 falls back to `5173` when env port is invalid/missing for script generation.
- Result:
  - Different port selection dynamics than `main`; may expose latent host-resolution issues more often.

7. Vite host-family ambiguity itself is not a new regression from this refactor.
- `main` and current both:
  - launch Vite without `--host` (`main:kit/viteutil/cmd.go:89-93`, [cmd.go](/Users/sjc/__code/river/lab/vitecmd/cmd.go):107-111),
  - generate `http://localhost:<port>` script URLs (`main:kit/viteutil/viteutil.go:133-143`, [viteutil.go](/Users/sjc/__code/river/lab/viteutil/viteutil.go):152-163).
- Result:
  - This remains a known ambiguity path, but it is not newly introduced by the refactor.

Live corroboration from current session:
- Rendered HTML currently contains `refreshWebSocketURL.port = String(10000);` and Vite script tags at `http://localhost:5174/...`.
- This is consistent with the mismatch risks above when sidecar is not on `10000` or localhost resolves differently.

If you want, I’ll implement a minimal hardening patch set next:
1. Set `__WAVE_REFRESH_SERVER_PORT` from actual started refresh port in `StartRefreshServer`.
2. Make refresh listener and refresh script host strategy consistent (dual-stack or explicit host).
3. Restore fail-fast for Vite/app startup failure (or gate with explicit mode flag).
4. Add regression tests for fallback refresh-port propagation and host-family consistency.
WAVE/VITE STRICT REGRESSION CHECKLIST (2026-03-01)

Context:
- Goal: strict behavioral parity/stability vs main for dev runtime.
- A/B bind test result: rolldown-vite 7.1.14 and 7.3.1 behaved the same under Node v24.13.1 when started without --host (IPv6 loopback bind, 127.0.0.1 refused).

Checklist (ordered hardest/systemic first):

[x] Restore fail-fast lifecycle semantics for runtime startup (do not continue in degraded state when Vite or app startup fails).
    - New behavior to remove: wave/wavedev/devserver/devserver.go (startRunCycleRuntime, StartApp, StartVite) logs and continues.
    - Old behavior references: main:wave/internal/ki/dev_core.go, main:wave/internal/ki/dumb_little_helpers.go.
    - Validation: inject startup failure and confirm run loop exits loudly instead of continuing.

[x] Fix refresh sidecar port propagation so runtime always uses the actual started sidecar port.
    - Issue: refresh server sets server.RefreshPort but runtime path does not set __WAVE_REFRESH_SERVER_PORT; script can fallback to 10000.
    - Affected: wave/wavedev/devserver/devserver.go, wave/wave.go.
    - Validation: rendered runtime script port always matches started sidecar port.

[x] Fix refresh host/bind consistency to eliminate IPv4/IPv6 loopback mismatch flakes.
    - Issue: sidecar binds 127.0.0.1:<port> while browser host may resolve localhost/::1.
    - Affected: wave/wavedev/devserver/internal/reloadwait/reloadwait.go, wave/internal/waveruntime/waveruntime.go.
    - Validation: deterministic success for localhost, 127.0.0.1, and [::1] page access modes.

[x] Reconcile readiness-failure handling with strict behavior (no silent skip/broadcast suppression on readiness errors).
    - Issue: readiness errors log warning and skip broadcast.
    - Affected: wave/wavedev/devserver/internal/reloadwait/reloadwait.go.
    - Old reference: main:wave/internal/ki/dumb_little_helpers.go.
    - Validation: forced readiness failure follows deterministic failure path.

[x] Reconcile readiness wait/backoff policy drift from old short budget to current long budget.
    - Current default: maxWait=25s with exponential backoff.
    - Old behavior: tighter ~3s budget.
    - Affected: wave/wavedev/devserver/internal/appsupervisor/appsupervisor.go.
    - Validation: cold/warm rebuild latency and failure timing match intended strict policy.

[x] Resolve Vite port policy drift (InitPort semantics and fallback behavior) to avoid new collision/flaky profiles.
    - New behavior: InitPort(defaultPort) honors caller default; script fallback can use 5173.
    - Old behavior: initialization started from 5199 regardless of caller default.
    - Affected: lab/viteutil/viteutil.go, old ref main:kit/viteutil/viteutil.go.
    - Validation: repeatable multi-run/multi-process startup without accidental collisions.

[x] Investigate and fix .well-known/appspecific request bleed into markdown sitemap/page-detail pathing.
    - Symptom: /.well-known/appspecific/com.chrome.devtools.json triggers markdown dir/sitemap errors and loader error path.
    - Validation: devtools probe requests no longer trigger page-detail generation errors.

[x] Decide and enforce deterministic Vite client URL host policy (even if not a new-vs-old delta) to remove resolver-dependent behavior.
    - Context: old and new both rely on localhost script URLs with no explicit Vite host.
    - Validation: no @vite/client connection refusal under local resolver/address-family variance.

Guardrail tests to add while fixing:

[x] Add non-E2E regression tests for each fixed behavioral bug path (startup fail-fast, refresh port propagation, host/bind consistency, readiness error semantics, and .well-known isolation).
[x] Keep tests black-box where possible and avoid implementation mirroring.

Reference copy (markdown format):
- __tmp/wave_vite_strict_regression_checklist_2026-03-01.md

---
FIRST-PRINCIPLES AUDIT ADDITIONS (2026-03-01)

[x] Browser action precedence can downgrade explicit hook reloads.
    - `requestBrowserAction` picks the numerically larger action, and `InvalidateVite` is ordered above `HardReload`.
    - If a pre-hook sets `ReloadBrowser=true` (hard reload) and implicit work later resolves to public-static invalidation, the final action can become invalidate.
    - Refs: wave/wavedev/devserver/internal/eventpipeline/eventpipeline.go:82-93,701-704,955-965; wave/wavedev/devserver/internal/runloop/runloop.go:230-255.

[x] Hook stage failure policy is fail-open by default, and invalid policy strings silently become fail-open.
    - Refs: wave/wavedev/devserver/internal/hooks/hooks.go:791-804; wave/wavedev/devserver/internal/runloop/runloop.go:359-367.
    - Tests encode this behavior: wave/wavedev/devserver/internal/eventpipeline/events_pipeline_decisions_test.go:721-852.

[x] Config reload errors are intentionally fail-open and keep running with stale config.
    - Refs: wave/wavedev/devserver/devserver.go:903-913.
    - Tests encode this behavior: wave/wavedev/devserver/devserver_run_test.go:2222-2239.

[x] Config parse/validation errors during watcher-side reload trigger config-restart flow even when reload failed.
    - This combines restart churn with stale-config continuation.
    - Refs: wave/wavedev/devserver/devserver.go:1673-1677,1729-1737; wave/wavedev/devserver/internal/runloop/runloop.go:148-152.

[x] Readiness default treats all 2xx/3xx statuses as ready.
    - Redirects/misroutes can be treated as healthy.
    - Ref: wave/wavedev/devserver/internal/appsupervisor/appsupervisor.go:56-57.

[x] Vite port API contract drift is internally inconsistent.
    - `BuildCtxOptions.DefaultPort` still exists and is passed through, but `viteutil.InitPort` ignores caller default and always probes from framework baseline.
    - Refs: lab/vitecmd/cmd.go:48-61,100; lab/viteutil/viteutil.go:19,198-199.
    - Tests encode conflicting expectations in different layers: lab/viteutil/viteutil_test.go:11-37 and lab/vitecmd/cmd_test.go:44-48.

[x] Runloop start-app path still panics from watcher pipeline instead of returning structured lifecycle error.
    - Ref: wave/wavedev/devserver/devserver.go:311-314.

[x] Reload HTTP calls have no client timeout and rely solely on outer context cancellation.
    - A hung local handler can stall invalidate/reload flows until context cancellation.
    - Refs: wave/wavedev/devserver/internal/reloadwait/reloadwait.go:488,540.

[x] CycleVite behavior appears dead in production paths but still carries bespoke fallback semantics.
    - No non-test producer sets `Browser.CycleVite=true`, yet `reloadwait` includes fallback-to-payload policy for this path.
    - Refs: wave/wavedev/devserver/internal/eventpipeline/eventpipeline.go:98-101,975-995; wave/wavedev/devserver/internal/reloadwait/reloadwait.go:386-405,428-440.
