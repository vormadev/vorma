# P017 Report

Provenance note: delivered by the executor (Sonnet 5 subagent, 2026-07-02) in its final
message per the anti-truncation convention; Fable placed it verbatim (transport escaping
undone; the full per-claim tables preserved).

## What changed

- `docs/maintainer/ARCHITECTURE.md` — corrected two drifted claims, added two missing
  architecture-level facts, and expanded a stale "such as" list. No other file touched.

## Per-claim verification table

Every assertion in ARCHITECTURE.md (as it stood at session start), checked against current
shipped source. Verdicts: accurate / drifted (corrected) / missing (added) /
considered-not-added.

### Crate and package layout

| Claim                                                                                                                                                             | Verdict               | Citation                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vorma` = runtime + app declaration API; `AppConfig` → `App::from_config` → `RuntimeHost`                                                                         | accurate              | `crates/vorma/src/public_app.rs:172`                                                                                                                                                                                                                                                                                                                              |
| `vorma::middleware` — etag, secure headers, body limits, timeouts, compression, request id, panic recovery                                                        | drifted (corrected)   | `crates/vorma/src/middleware.rs:63,140,154,159,164,173,205,650` all present; but `response_body_timeout_with_etag` (line 564, landed P010) was missing from the list                                                                                                                                                                                              |
| `vorma::build_interface` — named contract vorma-build consumes, version-locked                                                                                    | accurate              | `crates/vorma/src/lib.rs:740`                                                                                                                                                                                                                                                                                                                                     |
| `vorma::__private` — macro ABI only, doc(hidden), path-stable                                                                                                     | accurate              | `crates/vorma/src/lib.rs:843-848`                                                                                                                                                                                                                                                                                                                                 |
| (implicit) `vorma`'s full "Also exposes" list is exhaustive of app-facing re-export modules                                                                       | missing (added)       | `vorma::tasks` (`crates/vorma/src/lib.rs:340-347`) — a real, documented, public re-export of the entire `vorma-tasks` API under the app's namespace — was completely absent from the list                                                                                                                                                                         |
| `vorma-contract` module list: `framework_graph`, `execution_plan`, `runtime_manifest`, `contracts`, `tsgen`, `document_renderer`, `wire`, `live_state`, constants | accurate              | `crates/vorma-contract/src/*.rs` directory listing matches exactly; `extern crate self as vorma` confirmed at `crates/vorma-contract/src/lib.rs:55`                                                                                                                                                                                                               |
| `vorma-build` — no CLI; public surface is `run(app_config)` (+ `build_production`/`start_dev_server` for harnesses)                                               | accurate              | `crates/vorma-build/src/lib.rs:77` (`pub use entrypoint::run`); `crates/vorma-build/src/entrypoint.rs:1-8` module doc states verbatim that only `run` is public, the rest is crate-internal                                                                                                                                                                       |
| `vorma-macros` — `TsGen` derive + view/resource macros; diagnostics compile-fail-pinned in `crates/vorma/tests/compile_fail/`                                     | accurate              | directory confirmed present with `.rs`/`.stderr` pin pairs                                                                                                                                                                                                                                                                                                        |
| `vorma-matcher`, `vorma-tasks`, `vorma-client-wasm` — standalone matcher/tasks/WASM bridge                                                                        | accurate              | no source changes found affecting this framing                                                                                                                                                                                                                                                                                                                    |
| `packages/vorma` — `core/`, `ui/{react,preact,solid}`, `vite/`, `kit/`; `*.gen.ts` goldens in `core`/`vite`, `VORMA_UPDATE_WIRE_CONTRACTS=1` regenerates          | accurate              | `packages/vorma/ui/{preact,react,solid}` confirmed; `core/wire_contracts.gen.ts` + `vite/plugin_contract.gen.ts` confirmed as the only two crate-scoped goldens (app-facing `vorma.gen.ts` files are a distinct mechanism, correctly not conflated); env key confirmed at `crates/vorma-build/src/vite_plugin_ts_contracts.rs:180` and `wire_ts_contracts.rs:153` |
| (not previously claimed) `crates/vorma-bench` crate existence                                                                                                     | considered, not added | `publish = false` internal Go-style bench harness (`crates/vorma-bench/Cargo.toml`), not a shipped crate — the layout section is describing the shipped architecture, so omission is defensible, not drift                                                                                                                                                        |

### The single-binary model

| Claim                                                                                                                                               | Verdict                                  | Citation                                                                                                                                                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| One app-linked binary, two modes switched by one env key                                                                                            | accurate                                 | `crates/vorma/src/live_state_emit.rs`                                                                                                                                                                                                                                           |
| Live-state mode (`__VORMA_LIVE_BUILD_STATE=1`): `App::from_config` serializes graph + root-document hash source to stdout and exits                 | accurate                                 | `crates/vorma-contract/src/live_state.rs:47` (env key), `crates/vorma/src/public_app.rs:172-176`, `crates/vorma-contract/src/live_state.rs:55,62,67,96` (root_document_hash_source)                                                                                             |
| Errors travel as an error envelope with exit 0; build side distinguishes by payload                                                                 | accurate                                 | `crates/vorma/src/live_state_emit.rs:1-9,27-43` (doctrine comment + `emit_live_build_state_and_exit`, unconditional `std::process::exit(0)`)                                                                                                                                    |
| Per-app build entry (`src/bin/build.rs`) is a thin shim calling `vorma_build::run(app_config)`; never answers live-state, never rebuilt mid-session | accurate                                 | `examples/board/src/bin/build.rs:1-6`                                                                                                                                                                                                                                           |
| Process roles: one cargo compile (`cargo build -p <server-pkg> --bin <server-bin>`), server binary run once for live state, run again to serve      | accurate                                 | `crates/vorma-build/src/app_server_build.rs:1-7,25-34` (doctrine comment + doc comment match verbatim)                                                                                                                                                                          |
| `ServerBuildTarget` read in-process at session start (Go bootstrap pattern); mid-session target change fails the rebuild with a restart instruction | accurate — and stronger than paraphrased | `crates/vorma-build/src/entrypoint.rs:417-431` — the actual error text: `"server cargo target changed (was {..}/{..}, now {..}/{..}); restart the dev server to pick it up"`; doctrine comment at same location; corroborated at `crates/vorma-build/src/build_plan.rs:139-143` |

### Dev loop

| Claim                                                                                                                                                                                                                               | Verdict                                                                                                    | Citation                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `notify` watcher, session-static OS roots, swappable classification plan (`DevFileWatchPlanHandle`)                                                                                                                                 | accurate                                                                                                   | `crates/vorma-build/src/dev_watcher.rs:39-46,370-384`                                                                                                                                     |
| Changes coalesce in the loop (`drain_pending`)                                                                                                                                                                                      | accurate (function name is `drain_pending_dev_loop_action`, lives in `entrypoint.rs`, matching "the loop") | `crates/vorma-build/src/entrypoint.rs:583,587-607`                                                                                                                                        |
| App-server-class changes cancel the in-flight build; static/revalidate changes queue and merge                                                                                                                                      | accurate                                                                                                   | `crates/vorma-build/src/entrypoint.rs:542-565` — `ClientRevalidation`/`StaticGeneration` queue-and-extend without cancel; the else branch (`AppServerGeneration`) calls `cancel.cancel()` |
| Work classification: server recompile → full generation; public-static/critical-CSS → static fast path; client-revalidate → broadcast only                                                                                          | accurate                                                                                                   | `crates/vorma-build/src/entrypoint.rs:303-324` (`classify_dev_rebuild_work`, 3-way `DevRebuildWork` split)                                                                                |
| Child processes exit-watched without reaping (linux `waitid`+WNOWAIT, macOS kqueue NOTE_EXIT), terminate-intent flag, unexpected death fails session loudly                                                                         | accurate                                                                                                   | `crates/vorma-build/src/process_runner.rs:465-476` (WNOWAIT/reaping doctrine), `:499-514` (linux waitid), `:516-552` (macOS kqueue/NOTE_EXIT), `:1184` (terminate-intent)                 |
| Generations epochal: candidate prepared+published before commit; dev mux atomically switches (zero-downtime, better than Go's stop-then-start); failed activations roll back disk+contract state, previous generation keeps serving | accurate                                                                                                   | `crates/vorma-build/src/generation_epoch.rs:1-11` (transactional two-state model doc), `crates/vorma-build/src/output_lock.rs:66-70` (rollback doctrine comment, verbatim match)          |
| Orphaned previous output dir deleted only after `dist_dir` move commits, never at build time                                                                                                                                        | accurate                                                                                                   | `crates/vorma-build/src/dev_build.rs:713-724,775-786` (`orphaned_output_layout`, `remove_dir_all` gated on commit)                                                                        |

### Vite freshness model

| Claim                                                                                                                                         | Verdict                                                                         | Citation                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| One Vite plugin, dev and prod; resolves `vormaPublicUrl()`/`url(...)` via JIT `hash` RPC; records asset→module edges                          | accurate                                                                        | `packages/vorma/vite/vite.ts:42-55` (`fetch_public_url`/`rpc_url`), `:102-115` (`public_asset_consumers` map + `record_asset_consumer`)                                                                                                                                                                                                                                                                                             |
| Public-asset saves never restart Vite; `/assets-changed` invalidates exactly recorded consumer modules                                        | accurate                                                                        | `crates/vorma-build/src/vite_plugin_contract.rs:36-40`; `packages/vorma/vite/vite.ts:254-260,293-312` (`invalidateModule` calls)                                                                                                                                                                                                                                                                                                    |
| Retained stale hashed outputs keep already-cached transforms resolving                                                                        | accurate                                                                        | `crates/vorma-build/src/static_outputs.rs:99-111` (doctrine comment on `retained_stale_files`, verbatim-matching rationale)                                                                                                                                                                                                                                                                                                         |
| `/cfg-changed` fires only when `VitePluginConfig` payload changed (entry module, view-module set, ignored patterns, dedupe list, public base) | accurate — and more precise than the crate's OWN internal doc comment           | `crates/vorma-build/src/vite_plugin_contract.rs:51-62` — `VitePluginConfig` actually has 5 fields; the crate's own doc comment on `VITE_PLUGIN_CONFIG_CHANGED_PATH` (lines 30-34) only lists 4, omitting `public_static_base_path` — a bug in the source's OWN doc comment, out of this packet's authority to touch (source file). ARCHITECTURE.md's "public base" fifth item is the accurate one; left as-is, no doc change needed |
| Closes Go-era staleness/404 window; removes restart-per-rebuild bluntness                                                                     | accurate (design-rationale framing, not independently falsifiable against code) | consistent with the mechanism verified above                                                                                                                                                                                                                                                                                                                                                                                        |

### Runtime

| Claim                                                                                                                                                                                                                                                                                                                                                  | Verdict                                                                                                   | Citation                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `RuntimeHost` serves from manifest: hashed public statics (immutable cache headers), document shell (CSP-hashable critical CSS), execution engine driving middlewares → view/resource handlers with parallel client-loader semantics                                                                                                                   | accurate                                                                                                  | `crates/vorma/src/asset_capabilities.rs:82` + `crates/vorma/src/runtime_app.rs:1199` (`"public, max-age=31536000, immutable"`); `crates/vorma-contract/src/runtime_manifest.rs:284` (`critical_css_content_sha256`)                                                                                                                                                                                                      |
| (P004 Part 2) snapshot-precomputed document fragments + single-invocation execution fast path                                                                                                                                                                                                                                                          | missing (added)                                                                                           | `crates/vorma/src/runtime_snapshot.rs:19,52,77,114-115` (`PrecomputedViewFragments` field, computed once in `compile()`); `crates/vorma/src/execution_engine.rs:973-988` (fast-path doctrine comment, verbatim-matching); document-provider distinctness confirmed via `examples/board/src/document.rs:12,19` (`data("request-path", ...)`, a genuinely per-request value the precompute deliberately excludes)          |
| Views: framework-owned rendering protocol; `ViewExit` (explicit `client_msg` or generic; page still commits, HTTP 200); only framework faults 500 on view path                                                                                                                                                                                         | accurate                                                                                                  | `crates/vorma/src/exit.rs:1-15` (module doc); `crates/vorma/src/response_finalizer.rs:395-403` (`Response::new(body)` defaults to 200 when `effects.status` is `None`)                                                                                                                                                                                                                                                   |
| Resources/middlewares: real HTTP via `HttpExit` (status + client msg); resource-path errors reach the wire as `{"error": text}`; TS client parses it                                                                                                                                                                                                   | accurate                                                                                                  | `crates/vorma/src/resource_response.rs:89` (`json!({ "error": error_text })`); `crates/vorma/src/runtime_app.rs:581`; `packages/vorma/core/api_client.ts:48` (`super(result.error)`)                                                                                                                                                                                                                                     |
| Redirects ride the exit channel (`ctx.redirect`); statuses never control flow; terminal = engine-owned error flag or real redirect                                                                                                                                                                                                                     | accurate                                                                                                  | `crates/vorma/src/response_finalizer.rs:150,159-168` (`redirect`/`redirect_with_status` set `effects.status`)                                                                                                                                                                                                                                                                                                            |
| Middlewares selected per request AFTER route matching; optional plural `patterns`/`methods` filters (omitted=unrestricted, AND together); compiled into per-middleware flat matchers in ExecutionPlan; boolean path/method check, declaration order preserved; run as parallel pre-handler phase sharing the request's single task scope with handlers | accurate                                                                                                  | `crates/vorma-contract/src/framework_graph.rs:541-565` (`MiddlewareDeclaration` fields/builders); `crates/vorma-contract/src/execution_plan.rs:31-39,301-311` (module doc + `MiddlewarePlanNode` comment, both verbatim-matching); `crates/vorma/src/execution_engine.rs` tests `route_and_middleware_share_one_request_task_scope`, `middleware_invocations_execute_in_parallel_before_handler_phase` (lines 1700,1826) |
| No route match → no Vorma middlewares; transport-level concerns belong to tower/axum layer                                                                                                                                                                                                                                                             | accurate (consistent with matching-then-selecting architecture verified above; no counter-evidence found) | —                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Dev-only pieces (refresh websocket, `/.vorma/healthz`, build overlays) env-gated, compiled out of prod path where possible                                                                                                                                                                                                                             | accurate                                                                                                  | `crates/vorma-contract/src/constants.rs` (`DEV_HEALTH_PATH = "/.vorma/healthz"`, exact string match); `crates/vorma-build/src/dev_refresh.rs:14,22-25,75-81,301` (WebSocket + overlay payload kinds); client-side dev-gating via `import.meta.env.DEV`/`import.meta.hot` confirmed at `packages/vorma/core/modules.ts:4` and `packages/vorma/vite/vite.ts:63-64`                                                         |

### Client core

| Claim                                                                                                                                                                                            | Verdict             | Citation                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/vorma/core` decomposed into focused modules: revalidation scheduler, work projection, redirects, wire payload, client loaders, route modules, submissions, history position, head, css | accurate            | all 10 named files present                                                                                                                                                          |
| Old monolithic test suite decomposed into "such as" list of 5 suites                                                                                                                             | drifted (corrected) | actual current set is 7 files: the 5 named plus `core_hmr.test.ts` and `core_prefetch_and_css.test.ts`, both missing from the "such as" list (nearly 30% of the actual set omitted) |

### Known asymmetries (deliberate)

| Claim                                                                                                                                                 | Verdict                                                                                 | Citation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Windows: no child-exit watcher yet (needs SYNCHRONIZE-handle wait); dev works, death detection degrades to next-request failure                       | accurate                                                                                | `crates/vorma-build/src/process_runner.rs:465-476` (`#[cfg(windows)] fn watch_unexpected_exit_impl` is a no-op; doctrine comment cites the `windows-child-exit-watcher` ticket)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| OS watch-root set is session-static; a brand-new watch ROOT needs a dev restart — same reach as Go, "guarded by the explicit config-transition error" | drifted (corrected) — the "guarded by" clause described a mechanism that does not exist | `crates/vorma-build/src/dev_watcher.rs:352-362` — notify roots are bound once when `StartedDevFileWatcher` is created; the doctrine comment states verbatim that replacing the plan swaps only event classification, never the OS watcher. `DevWatcherError`'s variants contain nothing that detects a watch-root change mid-session; `WatchRoot` only fires on OS-level registration failure at watcher startup. `crates/vorma-build/src/entrypoint.rs:279` (`watch_plan_handle.replace(...)`) swaps only the classification plan on every rebuild, never the registered root set. No ticket tracks this specific gap (`dev-watch-excludes-not-pruned-from-os-watch` is a related-but-different concern about excluded subtrees, not root changes). Net effect: if a config change altered watch roots mid-session, the code would silently keep watching the stale roots — nothing errors or guards this today. Corrected in place; the corrected text explicitly contrasts this with the ServerBuildTarget mechanism (verified real, above) to make the asymmetry between the two "restart needed" cases clear rather than implying parity |
| `runtime_*` module prefix in `vorma` is deliberate: groups crate-private runtime layers, distinct from public app-declaration surface                 | accurate                                                                                | `crates/vorma/src/lib.rs:165-170` (all private `mod`, not `pub mod`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Missing facts considered and NOT added (with reasoning)

- **P019 TsGen shared-type dedup.** Real, landed, and named in the packet's own context.
  Not added because it sits below ARCHITECTURE.md's own established granularity: the
  document names the `contracts`/`tsgen` modules at the crate-layout level without
  describing any validation rule inside them; no other graph-validation rule gets a
  paragraph either. Already fully documented at the correct altitude in
  `TypeDef::classify_shared_name_with`'s doctest and the
  `GraphError::DivergentTypePhaseShapes` variant doc. Flagged as a judgment call the
  maintainer may want to override — if name-collision behavior is architecture-level (it
  does change what "duplicate type name" means at the public app API surface), a
  one-clause addition to the `vorma-contract` bullet would be the right shape.
- **The board maintenance-worker pattern** (P006). Not added: ARCHITECTURE.md contains
  zero example-app content of any kind. The general architectural fact it demonstrates —
  `vorma-tasks`' `Tasks`/`ExecCtx` usable standalone, outside any request — is precisely
  why the new `vorma::tasks` re-export bullet was added instead; the worked example
  belongs in Board's own source comments, which P006 delivered.
- **`crates/vorma-bench`.** `publish = false` internal bench harness; the layout section
  describes the shipped architecture, consistent with never mentioning `xtask` or
  `tests/framework` (also workspace members) either.

## Decisions made

1. Scope of "missing facts" additions kept deliberately narrow — two additions
   (`vorma::tasks` re-export, P004 Part 2 fast path), both citing real shipped code, both
   at the document's existing abstraction level; three candidates considered and
   explicitly NOT added with reasoning recorded, rather than silently expanding the
   document's scope.
2. The `VitePluginConfig` "public base" claim was investigated as a possible drift and
   found ACCURATE — the crate's OWN internal doc comment at
   `vite_plugin_contract.rs:30-34` is the one that under-counts the struct's real 5
   fields, not ARCHITECTURE.md. A fix to that source comment is out of this packet's
   docs-only authority; flagged as discovered out-of-scope work.
3. Used a background research agent for one narrowly-scoped sub-question (existence of a
   watch-root config-transition error; the ServerBuildTarget error's exact text), then
   independently re-verified both findings against the cited file:line before using them
   to justify a content correction.
4. Did not touch the "Dev loop" section's session-static-OS-roots sentence — it makes no
   guard claim; the drift was specifically the "Known asymmetries" extra claim about a
   guard mechanism.

## Gate results

- Scoped oxfmt write on exactly the one file, twice: first pass one pure reflow change,
  diffed and confirmed NO content corruption (all three known defect classes checked and
  absent); second pass byte-identical — convergence confirmed.
- `make ts-fmt-check` GREEN (321 files).
- `cargo test -p vorma --lib` 228/228 (sanity signal; matches the recorded count).
- `git status`/`git diff --stat`: exactly one file touched, 26 insertions / 6 deletions;
  read-only git throughout.

## Benchmarks

Not applicable — docs-only packet.

## Escalations / open questions

1. Two "missing fact" candidates (P019 dedup, board maintenance-worker) investigated and
   deliberately NOT added, reasoning above — a maintainer/orchestrator ruling can land
   either as a one-clause/one-paragraph follow-up.
2. A genuine documentation bug found in shipped source (not ARCHITECTURE.md):
   `crates/vorma-build/src/vite_plugin_contract.rs:30-34` under-lists `VitePluginConfig`'s
   fields (4 of 5, missing `public_static_base_path`). Out of this packet's authority;
   flagged for follow-up.

## Discovered out-of-scope work

- The `vite_plugin_contract.rs:30-34` doc-comment under-listing, above — a one-line
  rustdoc fix for triage into a follow-up.
