# Vorma Board

Board is a community discussion app built with Vorma. It includes stories, comments,
users, moderation, docs pages, search, typed resources, form submissions, middleware,
tasks, generated TypeScript client code, and browser-bundled views.

From the repository root:

```sh
cargo run -p vorma-board-example dev
```

The command starts the Vorma dev server for Board.
